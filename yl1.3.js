const readline = require("node:readline");

const r1 = readline.createInterface({
    input: process.stdin,
    output: process.stdout,

});

r1.question("sisestage mitu korda äratada", (vastus)=> {
    function arata() {
        for (let i =0; i < vastus; i++);
    }
}

    arata();
    r1,Close();
    });